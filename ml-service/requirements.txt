fastapi>=0.110
uvicorn[standard]>=0.29
pandas>=2.0
numpy>=1.26
scikit-learn>=1.4
joblib>=1.3
# xgboost intentionally NOT listed here: the committed model
# (model/match_model.joblib) is a scikit-learn HistGradientBoostingClassifier
# ("sklearn-hgb" backend) and does not need xgboost to load or predict at
# serving time. xgboost is a heavy compiled package that's a common cause of
# build failures/timeouts on constrained free-tier hosts (Render free tier
# included) — install it only in a local/training environment if you're
# retraining with train_model.py and want to try the xgboost backend.
